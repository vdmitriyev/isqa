version: '3'

includes:
  pre-commit: ./taskfiles/Taskfile_pre_commit.yml
  
tasks:

  default:
    desc: lists all tasks
    silent: true
    cmds:
      - task --list-all
      - echo ""
      - echo "Example usage-> 'task check-virtualenv'"